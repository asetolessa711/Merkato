import React from 'react';

export default function AdminCategories() {
  return (
    <div style={{ padding: 20 }}>
      <h1>Catalog Categories (Admin)</h1>
      <p>Lightweight placeholder for managing the taxonomy tree, visibility, regions, locales, and attributes.</p>
      <ul>
        <li>Drag & drop to reorder (coming soon)</li>
        <li>Edit details and attributes (coming soon)</li>
        <li>Publish to update caches (coming soon)</li>
      </ul>
    </div>
  );
}
