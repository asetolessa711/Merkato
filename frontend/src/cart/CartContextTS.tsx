// @ts-nocheck
// Thin wrapper to the canonical JS implementation.
// Keeps a stable import path for any TSX references without introducing TS errors.
export { CartProvider, useCart } from './CartContext';

