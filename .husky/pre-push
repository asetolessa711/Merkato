#!/usr/bin/env sh

echo "husky pre-push: canonical workflow checks"
export CI=true
export NODE_ENV=test
export PRE_PUSH=true
export JEST_CLOSE_DB=true

npm run -s guard:focused || exit 1
npm run -s guard:lint || exit 1
npm run -s verify:core || exit 1
