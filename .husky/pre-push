#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "husky pre-push: guard + backend/frontend tests"
export CI=true
node scripts/check-focused-tests.js || exit 1

# Backend and frontend from their folders
npm --prefix backend test --silent || exit 1
npm --prefix frontend test --silent || exit 1
